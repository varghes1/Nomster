<div class="booyah-box col-xs-10 col-xs-offset-1">

	<h1>Add details to your new place!</h1>
	<%= simple_form_for @place do |f| %>
		<%= f.input :name %>
		<%= f.input :description %>
		<%= f.input :address %>
		<br />
		<%= f.submit 'Create', :class => 'btn btn-primary' %>
	<% end %>

</div>